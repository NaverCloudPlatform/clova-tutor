// 📝 Auto-generated by E2E-Autogen
// 🔧 Generated from: TC-4.1, TC-4.2, TC-4.3, TC-4.4, TC-4.5, TC-4.6, TC-4.7

import { test } from "@playwright/test";

test.skip("[TC-4.1] 채팅 입력", async ({ page }) => {
  await test.step.skip("[TC-4.1.1] 입력창에 한 줄로 된 텍스트 입력 -> 입력창에 텍스트 표시되고 전송 버튼이 활성화된다", async () => {
      // 📍 UI Path: 채팅방 > 채팅 입력

      // 🎬 When: 입력창에 한 줄로 된 텍스트 입력
    
      // ✅ Then: 입력창에 텍스트 표시되고 전송 버튼이 활성화된다

    });

  await test.step.skip("[TC-4.1.2] 입력창에 여러 줄로 된 텍스트 입력 -> 입력창에 텍스트 표시될때, 최대 넓이까지 채팅창이 확장되며 최대 넓이 이후에는 스크롤이 생성된다", async () => {
      // 📍 UI Path: 채팅방 > 채팅 입력

      // 🎬 When: 입력창에 여러 줄로 된 텍스트 입력
    
      // ✅ Then: 입력창에 텍스트 표시될때, 최대 넓이까지 채팅창이 확장되며 최대 넓이 이후에는 스크롤이 생성된다

    });

  await test.step.skip("[TC-4.1.3] 해당 채팅방의 입력 창에 텍스트를 작성 중인 상태 -> 채팅 기록 카드에 \"작성중\" 뱃지가 노출된다", async () => {
      // 📍 UI Path: 사이드바 > 채팅 기록 > 모든 채팅

      // 🎬 When: 해당 채팅방의 입력 창에 텍스트를 작성 중인 상태
    
      // ✅ Then: 채팅 기록 카드에 "작성중" 뱃지가 노출된다

    });
});

test.skip("[TC-4.2] 채팅 전송", async ({ page }) => {
  await test.step.skip("[TC-4.2.1] 전송 버튼 클릭 -> 입력한 채팅이 모델에 전송되며 보낸 메시지로 표시된다", async () => {
      // 📍 UI Path: 채팅방 > 채팅 입력 > 채팅 전송

      // 🎬 When: 전송 버튼 클릭
    
      // ✅ Then: 입력한 채팅이 모델에 전송되며 보낸 메시지로 표시된다

    });

  await test.step.skip("[TC-4.2.2] 엔터 키 입력 -> 입력한 채팅이 모델에 전송되며 보낸 메시지로 표시된다", async () => {
      // 📍 UI Path: 채팅방 > 채팅 입력 > 채팅 전송

      // 🎬 When: 엔터 키 입력
    
      // ✅ Then: 입력한 채팅이 모델에 전송되며 보낸 메시지로 표시된다

    });

  await test.step.skip("[TC-4.2.3] 마크다운 적용 텍스트 입력 > 전송 버튼 클릭 -> 보낸 메시지에 마크다운이 적용된다", async () => {
      // 📍 UI Path: 채팅방 > 채팅 입력 > 채팅 전송

      // 🎬 When: 마크다운 적용 텍스트 입력 > 전송 버튼 클릭
    
      // ✅ Then: 보낸 메시지에 마크다운이 적용된다

    });

  await test.step.skip("[TC-4.2.4] LaTeX 적용 텍스트 입력 > 전송 버튼 클릭 -> 보낸 메시지에 수식이 적용된다", async () => {
      // 📍 UI Path: 채팅방 > 채팅 입력 > 채팅 전송

      // 🎬 When: LaTeX 적용 텍스트 입력 > 전송 버튼 클릭
    
      // ✅ Then: 보낸 메시지에 수식이 적용된다

    });

  await test.step.skip("[TC-4.2.5] 채팅 전송 시 http 에러 발생 -> 전송 메시지에 \"다시보내기\" 버튼이 활성화되고, 클릭 시 유저 쿼리가 재전송된다.", async () => {
      // 📍 UI Path: 채팅방 > 채팅 입력 > 채팅 전송

      // 🎬 When: 채팅 전송 시 http 에러 발생
    
      // ✅ Then: 전송 메시지에 "다시보내기" 버튼이 활성화되고, 클릭 시 유저 쿼리가 재전송된다.

    });

  await test.step.skip("[TC-4.2.6] 채팅 스트림 도중 에러 발생 -> 채팅 에러 콜아웃이 활성화되고, 액션 버튼 클릭 시 유저 쿼리가 재전송된다.", async () => {
      // 📍 UI Path: 채팅방 > 채팅 입력 > 채팅 전송

      // 🎬 When: 채팅 스트림 도중 에러 발생
    
      // ✅ Then: 채팅 에러 콜아웃이 활성화되고, 액션 버튼 클릭 시 유저 쿼리가 재전송된다.

    });
});

test.skip("[TC-4.3] 수식 블럭 삽입", async ({ page }) => {
  await test.step.skip("[TC-4.3.1] 수학 튜터 메뉴 선택 -> 수식 블럭 삽입 버튼이 활성화된다", async () => {
      // 📍 UI Path: 채팅방(수학 튜터)

      // 🎬 When: 수학 튜터 메뉴 선택
    
      // ✅ Then: 수식 블럭 삽입 버튼이 활성화된다

    });

  await test.step.skip("[TC-4.3.2] 수식 블럭 삽입 버튼 클릭 -> 입력창에 수식 블럭이 표시되고 수식 입력 popover가 활성화되며 커서가 popover 내부로 이동한다.", async () => {
      // 📍 UI Path: 채팅방(수학 튜터) > 수식 블럭 삽입

      // 🎬 When: 수식 블럭 삽입 버튼 클릭
    
      // ✅ Then: 입력창에 수식 블럭이 표시되고 수식 입력 popover가 활성화되며 커서가 popover 내부로 이동한다.

    });

  await test.step.skip("[TC-4.3.3] 마우스 클릭을 사용해 수식 블럭 밖으로 커서 이동 -> 수식 입력 popover가 닫힌다", async () => {
      // 📍 UI Path: 채팅방(수학 튜터) > 수식 블럭 삽입

      // 🎬 When: 마우스 클릭을 사용해 수식 블럭 밖으로 커서 이동
    
      // ✅ Then: 수식 입력 popover가 닫힌다

    });

  await test.step.skip("[TC-4.3.4] 마우스 클릭을 사용해 수식 블럭 안으로 커서 이동 -> 수식 입력 popover가 열린다", async () => {
      // 📍 UI Path: 채팅방(수학 튜터) > 수식 블럭 삽입

      // 🎬 When: 마우스 클릭을 사용해 수식 블럭 안으로 커서 이동
    
      // ✅ Then: 수식 입력 popover가 열린다

    });

  await test.step.skip("[TC-4.3.5] 키보드 화살표를 사용해 수식 블럭 밖으로 커서 이동 -> 수식 입력 popover가 닫히며 수식 블럭 다음으로 커서가 이동한다", async () => {
      // 📍 UI Path: 채팅방(수학 튜터) > 수식 블럭 삽입

      // 🎬 When: 키보드 화살표를 사용해 수식 블럭 밖으로 커서 이동
    
      // ✅ Then: 수식 입력 popover가 닫히며 수식 블럭 다음으로 커서가 이동한다

    });

  await test.step.skip("[TC-4.3.6] 키보드 화살표를 사용해 수식 블럭 안으로 커서 이동 -> 수식 입력 popover가 열리며 수식 입력 popover 내부의 모든 텍스트가 select 처리된다", async () => {
      // 📍 UI Path: 채팅방(수학 튜터) > 수식 블럭 삽입

      // 🎬 When: 키보드 화살표를 사용해 수식 블럭 안으로 커서 이동
    
      // ✅ Then: 수식 입력 popover가 열리며 수식 입력 popover 내부의 모든 텍스트가 select 처리된다

    });

  await test.step.skip("[TC-4.3.7] 수식 블럭 안에 숫자/영문 입력 -> 수식 블럭 안에 숫자, 영문이 입력된다", async () => {
      // 📍 UI Path: 채팅방(수학 튜터) > 수식 블럭 삽입 

      // 🎬 When: 수식 블럭 안에 숫자/영문 입력
    
      // ✅ Then: 수식 블럭 안에 숫자, 영문이 입력된다

    });

  await test.step.skip("[TC-4.3.8] 수식 블럭 안에 한글 입력 -> 수식 블럭 안에 한글이 입력된다", async () => {
      // 📍 UI Path: 채팅방(수학 튜터) > 수식 블럭 삽입 

      // 🎬 When: 수식 블럭 안에 한글 입력
    
      // ✅ Then: 수식 블럭 안에 한글이 입력된다

    });

  await test.step.skip("[TC-4.3.9] 수식 블럭 안에 수학 및 논리 기호 입력 -> 수식 블럭 안에 수학 및 논리 기호 표시되고 띄어쓰기 불가 > 추가 입력 가능한 영역이 표시된다", async () => {
      // 📍 UI Path: 채팅방(수학 튜터) > 수식 블럭 삽입 

      // 🎬 When: 수식 블럭 안에 수학 및 논리 기호 입력
    
      // ✅ Then: 수식 블럭 안에 수학 및 논리 기호 표시되고 띄어쓰기 불가 > 추가 입력 가능한 영역이 표시된다

    });

  await test.step.skip("[TC-4.3.10] 수식 블럭 안에서 방향키 입력 -> 수식 블럭 안에서 커서가 이동한다", async () => {
      // 📍 UI Path: 채팅방(수학 튜터) > 수식 블럭 삽입 

      // 🎬 When: 수식 블럭 안에서 방향키 입력
    
      // ✅ Then: 수식 블럭 안에서 커서가 이동한다

    });

  await test.step.skip("[TC-4.3.11] 수식 블럭 안에서 백스페이스 입력 -> 수식 블럭 안에 텍스트가 지워진다", async () => {
      // 📍 UI Path: 채팅방(수학 튜터) > 수식 블럭 삽입 

      // 🎬 When: 수식 블럭 안에서 백스페이스 입력
    
      // ✅ Then: 수식 블럭 안에 텍스트가 지워진다

    });

  await test.step.skip("[TC-4.3.12] 수식 가상키보드 열기 -> 수식 입력 popover의 키보드 아이콘을 누르면 가상키보드가 열리며 채팅 입력 영역이 가상키보드 위로 밀려 올라간다", async () => {
      // 📍 UI Path: 채팅방(수학 튜터) > 수식 블럭 삽입 

      // 🎬 When: 수식 가상키보드 열기
    
      // ✅ Then: 수식 입력 popover의 키보드 아이콘을 누르면 가상키보드가 열리며 채팅 입력 영역이 가상키보드 위로 밀려 올라간다

    });

  await test.step.skip("[TC-4.3.13] 아이콘을 사용해 수식 가상키보드 닫기 -> 가상키보드가 열린 상태에서 수식 popover의 키보드 아이콘을 누르면 가상키보드가 닫힌다", async () => {
      // 📍 UI Path: 채팅방(수학 튜터) > 수식 블럭 삽입 

      // 🎬 When: 아이콘을 사용해 수식 가상키보드 닫기
    
      // ✅ Then: 가상키보드가 열린 상태에서 수식 popover의 키보드 아이콘을 누르면 가상키보드가 닫힌다

    });

  await test.step.skip("[TC-4.3.14] 포커스 해제를 통한 수식 가상키보드 닫기 -> 수식 입력 popover가 닫히면 가상키보드가 닫힌다", async () => {
      // 📍 UI Path: 채팅방(수학 튜터) > 수식 블럭 삽입 

      // 🎬 When: 포커스 해제를 통한 수식 가상키보드 닫기
    
      // ✅ Then: 수식 입력 popover가 닫히면 가상키보드가 닫힌다

    });

  await test.step.skip("[TC-4.3.15] 수식 블럭 안에서 가상 키 입력 -> 수식 블럭 안에 로마자/수학 및 논리 기호 표시되고 띄어쓰기 불가 > 추가 입력 가능한 영역이 표시된다", async () => {
      // 📍 UI Path: 채팅방(수학 튜터) > 수식 블럭 삽입 

      // 🎬 When: 수식 블럭 안에서 가상 키 입력
    
      // ✅ Then: 수식 블럭 안에 로마자/수학 및 논리 기호 표시되고 띄어쓰기 불가 > 추가 입력 가능한 영역이 표시된다

    });

  await test.step.skip("[TC-4.3.17] 수식 블럭 뒤에서 백스페이스 입력 -> 수식 블럭 전체가 지워진다", async () => {
      // 📍 UI Path: 채팅방(수학 튜터) > 수식 블럭 삽입 

      // 🎬 When: 수식 블럭 뒤에서 백스페이스 입력
    
      // ✅ Then: 수식 블럭 전체가 지워진다

    });
});

test.skip("[TC-4.4] 수식 삽입 전송", async ({ page }) => {
  await test.step.skip("[TC-4.4.1] 수식 블럭 안에 커서 위치 > 전송 버튼 클릭 -> 가상 키보드가 비활성화된다", async () => {
      // 📍 UI Path: 채팅방(수학 튜터) > 수식 블럭 삽입 

      // 🎬 When: 수식 블럭 안에 커서 위치 > 전송 버튼 클릭
    
      // ✅ Then: 가상 키보드가 비활성화된다

    });

  await test.step.skip("[TC-4.4.2] 수식 블럭 안에 커서 위치 > 엔터 키 입력 -> 수식 블럭 오른쪽으로 커서가 이동한다", async () => {
      // 📍 UI Path: 채팅방(수학 튜터) > 수식 블럭 삽입 

      // 🎬 When: 수식 블럭 안에 커서 위치 > 엔터 키 입력
    
      // ✅ Then: 수식 블럭 오른쪽으로 커서가 이동한다

    });
});

test.skip("[TC-4.5] 이미지 업로드", async ({ page }) => {
  await test.step.skip("[TC-4.5.1] 이미지 업로드 버튼 클릭 -> 파일 선택창 팝업이 노출된다", async () => {
      // 📍 UI Path: 채팅방 > 이미지 업로드

      // 🎬 When: 이미지 업로드 버튼 클릭
    
      // ✅ Then: 파일 선택창 팝업이 노출된다

    });

  await test.step.skip("[TC-4.5.2] 형식: PNG, JPG, JPEG -> 파일 선택 버튼이 활성화된다", async () => {
      // 📍 UI Path: 채팅방 > 이미지 업로드

      // 🎬 When: 형식: PNG, JPG, JPEG
    
      // ✅ Then: 파일 선택 버튼이 활성화된다

    });

  await test.step.skip("[TC-4.5.3] 0Byte 파일 제출 -> 에러 토스트를 표시한다", async () => {
      // 📍 UI Path: 채팅방 > 이미지 업로드

      // 🎬 When: 0Byte 파일 제출
    
      // ✅ Then: 에러 토스트를 표시한다

    });

  await test.step.skip("[TC-4.5.4] 20MB를 초과하는 파일 제출 -> 에러 토스트를 표시한다", async () => {
      // 📍 UI Path: 채팅방 > 이미지 업로드

      // 🎬 When: 20MB를 초과하는 파일 제출
    
      // ✅ Then: 에러 토스트를 표시한다

    });

  await test.step.skip("[TC-4.5.5] 가로, 세로가 5:1을 초과하는 파일(3000x300) 제출 -> 에러 토스트를 표시한다", async () => {
      // 📍 UI Path: 채팅방 > 이미지 업로드

      // 🎬 When: 가로, 세로가 5:1을 초과하는 파일(3000x300) 제출
    
      // ✅ Then: 에러 토스트를 표시한다

    });

  await test.step.skip("[TC-4.5.6] 가로, 세로가 1:5을 초과하는 파일(300x3000) 제출 -> 에러 토스트를 표시한다", async () => {
      // 📍 UI Path: 채팅방 > 이미지 업로드

      // 🎬 When: 가로, 세로가 1:5을 초과하는 파일(300x3000) 제출
    
      // ✅ Then: 에러 토스트를 표시한다

    });

  await test.step.skip("[TC-4.5.7] 긴 쪽이 2240px을 초과하는 파일(3000x600) 제출 -> 에러 토스트를 표시한다", async () => {
      // 📍 UI Path: 채팅방 > 이미지 업로드

      // 🎬 When: 긴 쪽이 2240px을 초과하는 파일(3000x600) 제출
    
      // ✅ Then: 에러 토스트를 표시한다

    });

  await test.step.skip("[TC-4.5.8] 짧은 쪽이 4px 미만인 파일(10x2) 제출 -> 에러 토스트를 표시한다", async () => {
      // 📍 UI Path: 채팅방 > 이미지 업로드

      // 🎬 When: 짧은 쪽이 4px 미만인 파일(10x2) 제출
    
      // ✅ Then: 에러 토스트를 표시한다

    });

  await test.step.skip("[TC-4.5.9] 형식: PNG, JPG, JPEG 크기: 0Byte 초과 20MB 이하 비율: 가로, 세로가 1:5 또는 5:1 이하 길이: 가로, 세로 중 긴 쪽은 2240px이하, 짧은 쪽은 4px 이상 -> 입력창에 이미지 첨부한 후, 이미지 업로드 버튼이 비활성화된다", async () => {
      // 📍 UI Path: 채팅방 > 이미지 업로드

      // 🎬 When: 형식: PNG, JPG, JPEG 크기: 0Byte 초과 20MB 이하 비율: 가로, 세로가 1:5 또는 5:1 이하 길이: 가로, 세로 중 긴 쪽은 2240px이하, 짧은 쪽은 4px 이상
    
      // ✅ Then: 입력창에 이미지 첨부한 후, 이미지 업로드 버튼이 비활성화된다

    });

  await test.step.skip("[TC-4.5.10] X 버튼 클릭 -> 첨부한 이미지 삭제한 후, 이미지 업로드 버튼이 활성화된다", async () => {
      // 📍 UI Path: 채팅방 > 이미지 업로드

      // 🎬 When: X 버튼 클릭
    
      // ✅ Then: 첨부한 이미지 삭제한 후, 이미지 업로드 버튼이 활성화된다

    });

  await test.step.skip("[TC-4.5.11] 이미지 업로드 실패 -> 첨부한 이미지 썸네일 위해 재시도 버튼이 표시되고, 버튼을 누르면 업로드를 다시 시도한다", async () => {
      // 📍 UI Path: 채팅방 > 이미지 업로드

      // 🎬 When: 이미지 업로드 실패
    
      // ✅ Then: 첨부한 이미지 썸네일 위해 재시도 버튼이 표시되고, 버튼을 누르면 업로드를 다시 시도한다

    });
});

test.skip("[TC-4.6] 이미지 전송", async ({ page }) => {
  await test.step.skip("[TC-4.6.1] 입력창에 이미지 첨부 > 전송 버튼 클릭 -> 채팅창의 보낸 메시지에 이미지가 표시된다", async () => {
      // 📍 UI Path: 채팅방 > 이미지 업로드 > 채팅 전송

      // 🎬 When: 입력창에 이미지 첨부 > 전송 버튼 클릭
    
      // ✅ Then: 채팅창의 보낸 메시지에 이미지가 표시된다

    });

  await test.step.skip("[TC-4.6.2] 보낸 메시지의 이미지 클릭 -> 이미지 보기가 활성화된다", async () => {
      // 📍 UI Path: 채팅방 > 이미지 업로드 > 채팅 전송

      // 🎬 When: 보낸 메시지의 이미지 클릭
    
      // ✅ Then: 이미지 보기가 활성화된다

    });
});

test.skip("[TC-4.7] AI 튜터에게 질문", async ({ page }) => {
  await test.step.skip("[TC-4.7.1] 텍스트 드래그 -> 텍스트를 드래그한 후 AI 튜터에게 질문 버튼 활성화된다", async () => {
      // 📍 UI Path: 채팅방 > AI 튜터에게 질문

      // 🎬 When: 텍스트 드래그
    
      // ✅ Then: 텍스트를 드래그한 후 AI 튜터에게 질문 버튼 활성화된다

    });

  await test.step.skip("[TC-4.7.2] AI 튜터에게 질문 버튼 클릭 -> 질문 버튼을 클릭하면 입력창에 드래드한 텍스트가 인용구로 표시된다", async () => {
      // 📍 UI Path: 채팅방 > AI 튜터에게 질문

      // 🎬 When: AI 튜터에게 질문 버튼 클릭
    
      // ✅ Then: 질문 버튼을 클릭하면 입력창에 드래드한 텍스트가 인용구로 표시된다

    });

  await test.step.skip("[TC-4.7.3] AI 튜터에게 질문 버튼 클릭 -> 입력창에 마크다운 문법이 사용된 그대로 텍스트 표시된다", async () => {
      // 📍 UI Path: 채팅방 > AI 튜터에게 질문

      // 🎬 When: AI 튜터에게 질문 버튼 클릭
    
      // ✅ Then: 입력창에 마크다운 문법이 사용된 그대로 텍스트 표시된다

    });
});