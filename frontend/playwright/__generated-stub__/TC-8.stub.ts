// 📝 Auto-generated by E2E-Autogen
// 🔧 Generated from: TC-8.1, TC-8.2, TC-8.3, TC-8.4, TC-8.5, TC-8.6, TC-8.7

import { test } from "@playwright/test";

test.skip("[TC-8.1] 학습노트 문제 저장", async ({ page }) => {
  await test.step.skip("[TC-8.1.1] 사용자가 저장하기 버튼 클릭 -> 해당 문제가 학습 노트에 카드로 저장되고, 저장 완료 토스트가 노출된다", async () => {
      // 📍 UI Path: 채팅 화면 > 학습 콘텐츠 영역

      // 🎬 When: 사용자가 저장하기 버튼 클릭
    
      // ✅ Then: 해당 문제가 학습 노트에 카드로 저장되고, 저장 완료 토스트가 노출된다

    });

  await test.step.skip("[TC-8.1.2] 동일 problem_id를 다른 chat_id에서 다시 저장하려고 시도 -> 학습노트에 이미 저장한 문제야! 복습을 위해 한번 더 저장할까?' 문구가 노출되며 저장 여부에 대해 재학습한다", async () => {
      // 📍 UI Path: 채팅 화면 > 학습 콘텐츠 영역

      // 🎬 When: 동일 problem_id를 다른 chat_id에서 다시 저장하려고 시도
    
      // ✅ Then: 학습노트에 이미 저장한 문제야! 복습을 위해 한번 더 저장할까?' 문구가 노출되며 저장 여부에 대해 재학습한다

    });
});

test.skip("[TC-8.2] 학습노트 페이지 이동", async ({ page }) => {
  await test.step.skip("[TC-8.2.1] 사용자가 학습 노트 페이지로 이동할 때 -> 저장된 문제 카드 리스트가 최신 상태로 렌더링된다", async () => {
      // 📍 UI Path: 사이드바 > 학습 노트 진입

      // 🎬 When: 사용자가 학습 노트 페이지로 이동할 때
    
      // ✅ Then: 저장된 문제 카드 리스트가 최신 상태로 렌더링된다

    });
});

test.skip("[TC-8.3] 문제 상태 검색 필터링", async ({ page }) => {
  await test.step.skip("[TC-8.3.1] 사용자가 필터 버튼 클릭 -> 정답·오답·복습 완료·풀지 않음 드롭다운 옵션이 노출된다", async () => {
      // 📍 UI Path: 학습 노트 페이지 > 필터 버튼

      // 🎬 When: 사용자가 필터 버튼 클릭
    
      // ✅ Then: 정답·오답·복습 완료·풀지 않음 드롭다운 옵션이 노출된다

    });

  await test.step.skip("[TC-8.3.2] 사용자가 풀이 상태 옵션 선택 -> 해당 상태의 문제 카드만 리스트에 표시된다", async () => {
      // 📍 UI Path: 학습 노트 페이지 > 필터 드롭다운

      // 🎬 When: 사용자가 풀이 상태 옵션 선택
    
      // ✅ Then: 해당 상태의 문제 카드만 리스트에 표시된다

    });

  await test.step.skip("[TC-8.3.3] 이미 선택된 옵션을 다시 클릭 -> 체크가 해제되고 전체 문제 리스트가 다시 노출된다", async () => {
      // 📍 UI Path: 학습 노트 페이지 > 필터 드롭다운

      // 🎬 When: 이미 선택된 옵션을 다시 클릭
    
      // ✅ Then: 체크가 해제되고 전체 문제 리스트가 다시 노출된다

    });
});

test.skip("[TC-8.4] 문제 재풀이", async ({ page }) => {
  await test.step.skip("[TC-8.4.1] 사용자가 저장된 문제 카드 클릭 -> 문제 정보가 및 풀이 상태가 표시된 재풀이 모드 다이얼로그가 노출된다", async () => {
      // 📍 UI Path: 학습 노트 페이지 > 문제 카드

      // 🎬 When: 사용자가 저장된 문제 카드 클릭
    
      // ✅ Then: 문제 정보가 및 풀이 상태가 표시된 재풀이 모드 다이얼로그가 노출된다

    });

  await test.step.skip("[TC-8.4.2] 문제 다이얼로그가 노출된 상태에서 새로고침 -> 새로고침해도 현재 열려있는 다이얼로그가 유지된다", async () => {
      // 📍 UI Path: 학습 노트 페이지 > 문제 카드

      // 🎬 When: 문제 다이얼로그가 노출된 상태에서 새로고침
    
      // ✅ Then: 새로고침해도 현재 열려있는 다이얼로그가 유지된다

    });

  await test.step.skip("[TC-8.4.3] 사용자가 다이얼로그에서 문제를 다시 풀고 답안을 제출 -> 채점이 수행되고 결과 상태가 업데이트된다", async () => {
      // 📍 UI Path: 학습 노트 > 재풀이 다이얼로그

      // 🎬 When: 사용자가 다이얼로그에서 문제를 다시 풀고 답안을 제출
    
      // ✅ Then: 채점이 수행되고 결과 상태가 업데이트된다

    });
});

test.skip("[TC-8.5] 학습 목표 달성", async ({ page }) => {
  await test.step.skip("[TC-8.5.1] 사용자가 학습 목표가 설정된 다이얼로그에서 문제를 풀고 정답을 맞춰 목표 달성 -> 진행률 100% 달성 시점에 축하 메시지와 함께 confetti 애니메이션이 나타난다 정답 표시와 함께 선택지 상단 힌트 및 하단에 해설 정보가 표시되고 ‘채점하기’ 버튼이 비활성화된다", async () => {
      // 📍 UI Path: 학습 노트 > 재풀이 다이얼로그

      // 🎬 When: 사용자가 학습 목표가 설정된 다이얼로그에서 문제를 풀고 정답을 맞춰 목표 달성
    
      // ✅ Then: 진행률 100% 달성 시점에 축하 메시지와 함께 confetti 애니메이션이 나타난다 정답 표시와 함께 선택지 상단 힌트 및 하단에 해설 정보가 표시되고 ‘채점하기’ 버튼이 비활성화된다

    });

  await test.step.skip("[TC-8.5.2] 사용자가 학습 목표가 설정된 다이얼로그에서 문제를 풀었지만 오답이었을 때 -> 오답 표시와 함께 선택지 상단 힌트가 표시되고 사용자는 다른 선택지를 다시 선택하여 채점하기 버튼을 클릭할 수 있다", async () => {
      // 📍 UI Path: 학습 노트 > 재풀이 다이얼로그

      // 🎬 When: 사용자가 학습 목표가 설정된 다이얼로그에서 문제를 풀었지만 오답이었을 때
    
      // ✅ Then: 오답 표시와 함께 선택지 상단 힌트가 표시되고 사용자는 다른 선택지를 다시 선택하여 채점하기 버튼을 클릭할 수 있다

    });

  await test.step.skip("[TC-8.5.3] 재풀이 채점 완료 시, 정답 또는 오답 피드백을 받았을 때 -> 문제 카드의 문제 상태 배지가 풀이 상태에 맞춰 업데이트된다", async () => {
      // 📍 UI Path: 학습 노트 > 재풀이 다이얼로그

      // 🎬 When: 재풀이 채점 완료 시, 정답 또는 오답 피드백을 받았을 때
    
      // ✅ Then: 문제 카드의 문제 상태 배지가 풀이 상태에 맞춰 업데이트된다

    });

  await test.step.skip("[TC-8.5.4] 다이얼로그 좌, 우의 페이지네이션 버튼 클릭 -> 양 옆의 문제로 페이지네이션이 가능하여 문제를 확인할 수 있다", async () => {
      // 📍 UI Path: 학습 노트 > 재풀이 다이얼로그

      // 🎬 When: 다이얼로그 좌, 우의 페이지네이션 버튼 클릭
    
      // ✅ Then: 양 옆의 문제로 페이지네이션이 가능하여 문제를 확인할 수 있다

    });
});

test.skip("[TC-8.6] 채팅방에서 질문하기", async ({ page }) => {
  await test.step.skip("[TC-8.6.1] 사용자가 ‘채팅방에서 질문하기’ 버튼 클릭 -> 해당 문제가 추천된 chat_id 기반 원본 채팅방으로 이동하여 대화를 지속할 수 있다", async () => {
      // 📍 UI Path: 학습 노트 페이지 > 문제 카드 > 채팅방에서 질문하기

      // 🎬 When: 사용자가 ‘채팅방에서 질문하기’ 버튼 클릭
    
      // ✅ Then: 해당 문제가 추천된 chat_id 기반 원본 채팅방으로 이동하여 대화를 지속할 수 있다

    });
});

test.skip("[TC-8.7] 북마크 설정", async ({ page }) => {
  await test.step.skip("[TC-8.7.1] 북마크가 켜져있는 상태에서 북마크 아이콘 클릭 -> 해당 문제의 북마크가 해제되며, 아이콘이 변경되고 카드 UI는 그대로 유지된다", async () => {
      // 📍 UI Path: 학습 노트 > 문제 카드

      // 🎬 When: 북마크가 켜져있는 상태에서 북마크 아이콘 클릭
    
      // ✅ Then: 해당 문제의 북마크가 해제되며, 아이콘이 변경되고 카드 UI는 그대로 유지된다

    });

  await test.step.skip("[TC-8.7.2] 북마크가 해제된 상태에서 북마크 아이콘 클릭 -> 해당 문제의 북마크가 다시 켜지며, 아이콘이 변경되고 카드 UI는 그대로 유지된다", async () => {
      // 📍 UI Path: 학습 노트 > 문제 카드

      // 🎬 When: 북마크가 해제된 상태에서 북마크 아이콘 클릭
    
      // ✅ Then: 해당 문제의 북마크가 다시 켜지며, 아이콘이 변경되고 카드 UI는 그대로 유지된다

    });

  await test.step.skip("[TC-8.7.3] 북마크가 해제된 상태에서 페이지 이동 혹은 새로고침 -> 북마크가 해제된 카드 UI가 사라진다", async () => {
      // 📍 UI Path: 학습 노트 > 문제 카드

      // 🎬 When: 북마크가 해제된 상태에서 페이지 이동 혹은 새로고침
    
      // ✅ Then: 북마크가 해제된 카드 UI가 사라진다

    });
});